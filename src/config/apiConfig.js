const API_CONFIG = {
    BASE_URL: process.env.REACT_APP_API_BASE_URL, // Replace with your actual API Gateway endpoint
    ENDPOINTS: {
      UPDATE_ROLE: '/update-role', // Relative path for the update role API
    },
  };
  
  export default API_CONFIG;